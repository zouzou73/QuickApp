/* Defines the doctor entity */
export interface IDoctor {
    doctortId: number;
    doctorName: string;
    doctorSpeciality: string;
    address: string;
    phoneNumber: number;
    starRating: number;
    imageUrl: string;
}